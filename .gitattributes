# Нормализация переводов строк: текстовые файлы хранятся в репозитории с LF
* text=auto

# Windows-файлы сохраняют CRLF
*.bat  text eol=crlf

# Бинарные файлы (если попадут в репозиторий)
*.png  binary
*.exe  binary
